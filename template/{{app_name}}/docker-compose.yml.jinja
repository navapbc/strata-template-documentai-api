services:
  {{app_name}}:
    build:
      context: .
      platforms:
        - linux/amd64
    ports:
      - {{ app_local_port }}:8000
    env_file: .env
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests
      - ~/.aws:/root/.aws:ro
    environment:
      - AWS_PROFILE